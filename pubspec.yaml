name: gorider
description: "Rider App For Go Sharp Sharp LTD."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  google_fonts: ^6.2.1
  get: ^4.7.2
  flutter_screenutil: ^5.9.3
  flutter_image_compress: ^2.4.0
  skeletonizer: ^1.4.2
  dotted_border: ^2.1.0
  flutter_dotenv: ^5.1.0
  get_storage: ^2.1.1
  upgrader:
  smooth_page_indicator: ^1.1.0
  cached_network_image: ^3.4.0
  dotted_line: ^3.2.3
  flutter_rating_bar: ^4.0.1
  flutter_ringtone_player:
  lottie: ^3.1.3
  pinput: ^5.0.1
  flutter_svg: ^2.0.10+1
  dio: ^5.4.0
  fl_chart: ^0.69.0
  get_it: ^8.2.0
  provider: ^6.1.2
  google_maps_flutter: ^2.9.0
  geolocator: ^9.0.2
  flutter_google_maps_webservices: ^1.1.1
  image_picker: ^1.1.2
  webview_flutter: ^4.8.0
  image_cropper: ^5.0.1
  intl_phone_field: ^3.1.0
  flutter_polyline_points: ^3.0.1
  cloudinary: ^1.2.0
  socket_io_client: ^3.0.2
  flutter_native_splash: ^2.4.0
  intl:
  cupertino_icons: ^1.0.8
  # Zego packages temporarily disabled due to compatibility issues
  # zego_uikit_prebuilt_call: 4.16.0
  # zego_uikit_signaling_plugin: ^2.8.0
  # zego_express_engine:
  # zego_zim: ^2.18.0
  # zego_zpns: ^2.7.0
  url_launcher: ^6.3.1
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.0
  json_serializable: ^6.0.1
  flutter_launcher_icons: ^0.14.1
  wakelock_plus: ^1.2.2

  flutter_lints: ^4.0.0


# The following section is specific to Flutter packages.
flutter:


  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/svgs/
    - assets/icons/
    - assets/imgs/
    - assets/json/


  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_native_splash:
  color: "#000000"
  image: "assets/icons/gosharp_icon.png"
  android: true
  ios: true
  web: false
  android_12:
    color: "#000000"
    image: "assets/icons/gosharp_icon.png"

dependency_overrides:
  image_cropper: ^5.0.1
  image_cropper_platform_interface: ^5.0.0
  floating: ^6.0.0
  flutter_ringtone_player: ^4.0.0+4
  vibration: ^3.1.4
  file_picker: 8.3.1
  # zego_uikit: 2.24.0
  # zego_plugin_adapter: 2.12.0
  # zego_callkit: 1.0.0+3
